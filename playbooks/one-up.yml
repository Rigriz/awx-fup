---
- hosts: L81
  become: yes
  tasks:
    - name: Run system upgrade reboot
      command: dnf system-upgrade reboot
#    - name: Update fedora repos first
 #     dnf:
#        name: fedora-gpg-keys
#        state: latest

 #   - name: Download Fedora upgrade
  #    command: >
  #      dnf system-upgrade download
   #     --releasever=43
    #    --allowerasing
     #   -y
